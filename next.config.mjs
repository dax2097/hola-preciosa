/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ["encrypted-tbn0.gstatic.com"], // Agrega aquí el dominio permitido
      },
};

export default nextConfig;
